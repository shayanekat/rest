# RestAPI en python

Ce projet me sert à apprendre comment fonctionne une API REST faite en python

le projet se lance avec la commande `uvicorn main:app --reload` une fois avoir installé les libs requises dans le fichier `requirements.txt` avec la commande `pip install -r requirements.txt`